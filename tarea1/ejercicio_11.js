//suma

const sumflecha =(a,b)=> a + b;
// Llamada a la función con dos números y muestra el resultado en la consola
const num1 = 10;
const num2 = 5;
const resultado = sumflecha(num1, num2);
console.log("La suma de " + num1 + " y " + num2 + " es: " + resultado);
