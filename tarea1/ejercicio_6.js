// Definición de un array de nombres
var nombres = ["Juan", "María", "Carlos", "Laura", "Pedro"];

// Utilizar un bucle for para recorrer el array 
for (var i = 0; i < nombres.length; i++) {
    console.log(nombres[i]);
}